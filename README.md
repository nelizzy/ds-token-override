# DS Token Override

## Goal
Provide an intuitive overlay for Foundry token
- healthbar (dead < dying < winded < full) -- lines at each status point
- shows temp HP on top of healthbar i.e. pf2e method?
- show health status text above health bar
- show numbers on healthbar on hover FOR HEROES ONLY
- add editable fields on token right-click for recoveries > heroic resource > surges
- show the three fields above on token without hovering (OPTIONAL)